# Быстрый старт - 5 минут до запуска

## Для кого эта инструкция?

Эта инструкция для тех, кто:
- Никогда не работал с терминалом/командной строкой
- Не знает, что такое Python
- Хочет быстро запустить систему и посмотреть, как она работает

## Что вам понадобится?

- Компьютер с Windows, Mac или Linux
- Интернет-соединение
- 10-15 минут свободного времени

## Шаг 1: Установка Python (5 минут)

### Windows:

1. Откройте браузер
2. Перейдите на: **https://www.python.org/downloads/**
3. Нажмите большую желтую кнопку "Download Python 3.x.x"
4. Откройте скачанный файл (обычно в папке "Загрузки")
5. **ВАЖНО!** В первом окне установщика поставьте галочку:
   ```
   ☑ Add Python to PATH
   ```
6. Нажмите "Install Now"
7. Дождитесь окончания (появится надпись "Setup was successful")
8. Нажмите "Close"

### Mac:

1. Откройте браузер
2. Перейдите на: **https://www.python.org/downloads/**
3. Нажмите "Download Python 3.x.x"
4. Откройте скачанный файл `.pkg`
5. Следуйте инструкциям установщика (просто нажимайте "Продолжить")
6. В конце введите пароль от вашего компьютера

### Linux (Ubuntu/Debian):

Откройте терминал (Ctrl+Alt+T) и выполните:

```bash
sudo apt update
sudo apt install python3 python3-pip
```

## Шаг 2: Проверка установки (1 минута)

1. Откройте терминал/командную строку:
   - **Windows**: Нажмите Win+R, введите `cmd`, нажмите Enter
   - **Mac**: Нажмите Cmd+Пробел, введите "Терминал", нажмите Enter
   - **Linux**: Нажмите Ctrl+Alt+T

2. Введите команду:
```bash
python --version
```

Должно появиться что-то вроде:
```
Python 3.9.7
```

**Если команда не работает, попробуйте:**
```bash
python3 --version
```

## Шаг 3: Скачивание проекта (2 минуты)

### Вариант А: Если проект уже на вашем компьютере

1. Откройте терминал
2. Перейдите в папку проекта:
```bash
cd путь/к/папке/anomaly
```
*(Замените "путь/к/папке/anomaly" на реальный путь к папке)*

**Как узнать путь к папке:**
- **Windows**: Откройте папку в проводнике, нажмите на адресную строку сверху - там будет путь. Скопируйте его.
- **Mac**: Откройте папку, нажмите Option+Правый клик на файл → "Скопировать путь"
- **Linux**: В проводнике правый клик на папку → "Свойства" → посмотрите "Расположение"

### Вариант Б: Если нужно скачать

1. Откройте терминал
2. Перейдите в папку, куда хотите скачать (например, Документы):
```bash
cd ~/Documents
```

3. Если у вас установлен Git, выполните:
```bash
git clone https://ваш-репозиторий/anomaly.git
cd anomaly
```

Если Git не установлен, просто скачайте архив проекта через браузер и распакуйте его.

## Шаг 4: Установка зависимостей (3 минуты)

В терминале (убедитесь, что вы в папке проекта) выполните:

```bash
pip install -r requirements.txt
```

**Если не работает, попробуйте:**
```bash
python -m pip install -r requirements.txt
```

или

```bash
python3 -m pip install -r requirements.txt
```

Эта команда может выполняться несколько минут. Вы увидите много строк с текстом - это нормально, просто подождите.

**Если видите ошибки** - см. раздел "Решение проблем" в конце.

## Шаг 5: Запуск системы (1 минута)

Все еще в терминале, выполните:

```bash
python main.py
```

Или:

```bash
python3 main.py
```

Вы увидите что-то вроде:
```
Initializing Anomaly Detection System...
Registered agent: ...
Starting monitoring...
Starting API server on 0.0.0.0:8000
```

**Поздравляю! Система запущена!** 🎉

## Шаг 6: Открытие веб-интерфейса (30 секунд)

1. Откройте браузер (Chrome, Firefox, Safari - любой)
2. В адресной строке введите:
```
http://localhost:8000
```

3. Нажмите Enter

Вы должны увидеть информацию о системе в формате JSON (это нормально - это данные в специальном формате).

### Полезные страницы:

- **Список агентов**: http://localhost:8000/agents
- **Найденные проблемы**: http://localhost:8000/anomalies
- **Статистика**: http://localhost:8000/stats
- **Проверка здоровья**: http://localhost:8000/health

## Шаг 7: Остановка системы

Когда захотите остановить систему:

1. Вернитесь в терминал
2. Нажмите `Ctrl+C` (на Mac это тоже Ctrl+C, а не Cmd)
3. Подождите несколько секунд

## Что дальше?

### Добавление вашего первого агента

1. Откройте файл `config/agents.yaml` в текстовом редакторе
2. Добавьте информацию о вашем агенте (см. примеры в файле)
3. Сохраните файл
4. Перезапустите систему (остановите Ctrl+C и запустите снова)

### Запуск примера с виртуальными агентами

В другом терминале (оставьте первый запущенным) выполните:

```bash
python examples/agent_simulator.py
```

Это создаст виртуальных агентов и будет генерировать проблемы, чтобы вы могли увидеть, как система их обнаруживает.

## Решение проблем

### "python: command not found" или "python: не является внутренней или внешней командой"

**Проблема:** Python не установлен или не найден.

**Решение:**
1. Убедитесь, что Python установлен (см. Шаг 1)
2. Попробуйте `python3` вместо `python`
3. Перезапустите терминал после установки Python

### "pip: command not found"

**Решение:** Используйте `python -m pip` вместо просто `pip`:

```bash
python -m pip install -r requirements.txt
```

### "Permission denied" или "отказано в доступе"

**Решение:** На Mac/Linux попробуйте с `sudo`:

```bash
sudo pip install -r requirements.txt
```

(Вас попросят ввести пароль)

### Порт 8000 занят

**Решение:** Используйте другой порт:

```bash
python main.py --port 8001
```

Затем откройте http://localhost:8001

### "No module named 'xxx'"

**Решение:** Не все пакеты установлены. Попробуйте установить заново:

```bash
pip install --upgrade -r requirements.txt
```

## Нужна помощь?

Если что-то не работает:

1. Скопируйте текст ошибки из терминала
2. Попробуйте найти решение в полной документации (README_SIMPLE.md)
3. Проверьте, что все шаги выполнены правильно

## Краткая памятка

```bash
# 1. Проверка Python
python --version

# 2. Переход в папку проекта
cd путь/к/anomaly

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Запуск
python main.py

# 5. Открыть в браузере
# http://localhost:8000

# 6. Остановка
# Ctrl+C в терминале
```

---

**Готово! Теперь вы знаете, как запустить систему.** 

Для более подробной информации см. файл README_SIMPLE.md

