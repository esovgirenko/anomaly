# Шпаргалка по командам

Быстрая справка по основным командам для работы с системой.

## 🔧 Установка и настройка

### Проверка Python
```bash
python --version
# или
python3 --version
```

### Переход в папку проекта
```bash
cd путь/к/папке/anomaly
```

### Установка зависимостей
```bash
pip install -r requirements.txt
# или
python -m pip install -r requirements.txt
```

### Обновление зависимостей
```bash
pip install --upgrade -r requirements.txt
```

---

## 🚀 Запуск системы

### Простой запуск
```bash
python main.py
```

### С настройками
```bash
python main.py --config config/config.yaml --agents-config config/agents.yaml
```

### На другом порту
```bash
python main.py --port 8001
```

### Без веб-интерфейса
```bash
python main.py --no-api
```

### Справка по командам
```bash
python main.py --help
```

---

## 🛑 Остановка

### Остановить систему
```
Нажмите Ctrl+C в терминале
```

---

## 🌐 Веб-интерфейс

### Основные адреса

| Адрес | Что показывает |
|-------|----------------|
| http://localhost:8000 | Главная страница API |
| http://localhost:8000/health | Проверка состояния |
| http://localhost:8000/agents | Список агентов |
| http://localhost:8000/anomalies | Список аномалий |
| http://localhost:8000/stats | Общая статистика |

### Аномалии с фильтрами

```bash
# За последние 24 часа
http://localhost:8000/anomalies?hours=24

# Для конкретного агента
http://localhost:8000/anomalies?agent_id=мой_агент

# Только серьезные (severity >= 0.7)
http://localhost:8000/anomalies?min_severity=0.7

# Комбинация фильтров
http://localhost:8000/anomalies?hours=12&agent_id=мой_агент&min_severity=0.8
```

### Статистика по агенту
```
http://localhost:8000/agents/ИМЯ_АГЕНТА/stats
```

---

## 📁 Работа с файлами

### Просмотр файлов (Mac/Linux)
```bash
ls              # Список файлов
ls -la          # Подробный список
cat файл.txt    # Просмотр содержимого
```

### Просмотр файлов (Windows)
```bash
dir             # Список файлов
type файл.txt   # Просмотр содержимого
```

### Копирование файла
```bash
# Mac/Linux
cp файл.txt новая_копия.txt

# Windows
copy файл.txt новая_копия.txt
```

### Создание папки
```bash
mkdir новая_папка
```

---

## 🐳 Docker команды

### Запуск
```bash
docker-compose up -d
```

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Перезапуск
```bash
docker-compose restart
```

### Остановка и удаление
```bash
docker-compose down -v
```

---

## 🔍 Полезные команды

### Проверка занятых портов

**Mac/Linux:**
```bash
lsof -i :8000
```

**Windows:**
```bash
netstat -ano | findstr :8000
```

### Поиск процесса Python

**Mac/Linux:**
```bash
ps aux | grep python
```

**Windows:**
```bash
tasklist | findstr python
```

### Просмотр переменных окружения

**Mac/Linux:**
```bash
env
echo $PATH
```

**Windows:**
```bash
set
echo %PATH%
```

---

## 📝 Работа с конфигурацией

### Редактирование файлов

**Windows:**
```bash
notepad config/config.yaml
```

**Mac:**
```bash
open -e config/config.yaml
```

**Linux:**
```bash
nano config/config.yaml
# или
vim config/config.yaml
```

### Копирование примера конфигурации

**Mac/Linux:**
```bash
cp config/agents.yaml.example config/agents.yaml
```

**Windows:**
```bash
copy config\agents.yaml.example config\agents.yaml
```

---

## 🐍 Python команды

### Проверка установленных пакетов
```bash
pip list
```

### Установка конкретного пакета
```bash
pip install имя_пакета
```

### Удаление пакета
```bash
pip uninstall имя_пакета
```

### Просмотр информации о пакете
```bash
pip show имя_пакета
```

---

## 📊 Примеры использования

### Запуск примера
```bash
python examples/basic_usage.py
```

### Запуск симулятора
```bash
python examples/agent_simulator.py
```

---

## ⚠️ Решение проблем

### Очистка кэша pip
```bash
pip cache purge
```

### Переустановка пакетов
```bash
pip install --force-reinstall -r requirements.txt
```

### Проверка синтаксиса Python файла
```bash
python -m py_compile файл.py
```

### Запуск Python в интерактивном режиме
```bash
python
# или
python3
```

---

## 📚 Справка по командам

### Получить справку по команде
```bash
команда --help
# или
команда -h
```

### Примеры:
```bash
python --help
pip --help
docker-compose --help
```

---

## 💡 Полезные сочетания клавиш в терминале

### Очистка экрана
```
Ctrl+L (Mac/Linux)
cls (Windows)
```

### Прервать выполнение
```
Ctrl+C
```

### Приостановить вывод
```
Ctrl+S
```

### Возобновить вывод
```
Ctrl+Q
```

### Завершить терминал
```
Ctrl+D (Mac/Linux)
exit (Windows)
```

### Автодополнение
```
Tab - автоматически дополняет команды и имена файлов
```

---

## 🎯 Частые задачи

### Полная переустановка зависимостей
```bash
pip uninstall -r requirements.txt -y
pip install -r requirements.txt
```

### Запуск с другим конфигом
```bash
python main.py --config другой_config.yaml
```

### Проверка версий всех компонентов
```bash
python --version
pip --version
git --version
docker --version
```

### Создание резервной копии конфигурации
```bash
# Mac/Linux
cp -r config config.backup

# Windows
xcopy config config.backup /E /I
```

---

**Совет:** Сохраните эту страницу в закладки для быстрого доступа! 📌

