# Система обнаружения аномалий для LLM-агентов - Простая инструкция

## Что это такое?

Эта система помогает следить за работой ваших **LLM-агентов** (программ на базе больших языковых моделей, таких как GPT-4, Claude и других) и автоматически находить проблемы (аномалии). 

LLM-агенты — это автономные системы, которые могут воспринимать окружение, планировать действия и достигать целей без жесткого программирования. Система следит за ними 24/7 и сообщает вам, если что-то идет не так: слишком высокая стоимость, низкое качество ответов, проблемы с производительностью и т.д.

## Основные понятия (простыми словами)

### LLM-Агент
Это ваша программа на базе большой языковой модели (LLM), за которой нужно следить. Например:
- Чат-бот на базе GPT-4 или Claude
- Система обработки документов с использованием LLM
- Автоматический ассистент для ответов на вопросы

LLM-агенты работают с языковыми моделями и имеют свои специфические метрики.

### Аномалия
Это когда LLM-агент работает не так, как обычно. Например:
- Агент стал отвечать слишком долго (высокая задержка)
- Слишком высокая стоимость использования API
- Снизилось качество ответов (галлюцинации, ошибки)
- Превышены лимиты на количество запросов
- Переполнено контекстное окно
- Слишком много токенов используется

### Метрики для LLM-агентов
Это числа, которые показывают, как работает ваш LLM-агент:
- **Токены**: количество используемых токенов (input/output/total)
- **Задержка (Latency)**: время ответа модели (в миллисекундах)
- **Стоимость (Cost)**: сколько стоит каждый запрос к API (в долларах)
- **Качество (Quality)**: оценка качества ответов (0.0 до 1.0)
- **Галлюцинации**: обнаружены ли галлюцинации в ответах
- **Контекстное окно**: сколько процентов контекстного окна используется
- **Rate Limits**: сколько запросов осталось до лимита

## Что нужно для начала работы

### Установка Python (если его еще нет)

1. Откройте браузер и перейдите на сайт: https://www.python.org/downloads/
2. Скачайте Python версии 3.9 или новее
3. Запустите установщик
4. **ВАЖНО**: В первом окне установщика поставьте галочку "Add Python to PATH"
5. Нажмите "Install Now"
6. Дождитесь окончания установки

**Как проверить, что Python установлен:**
- Откройте "Терминал" (Mac/Linux) или "Командную строку" (Windows)
- Напечатайте: `python --version`
- Должно появиться что-то вроде: `Python 3.9.7` или выше

### Установка Git (для скачивания проекта)

1. Перейдите на: https://git-scm.com/downloads
2. Скачайте установщик для вашей операционной системы
3. Запустите установщик и следуйте инструкциям (можно везде нажимать "Далее" с настройками по умолчанию)

## Установка системы (шаг за шагом)

### Шаг 1: Скачивание проекта

Откройте Терминал/Командную строку и выполните:

**Если у вас уже есть папка с проектом:**
```bash
cd путь/к/папке/anomaly
```

**Если нужно скачать проект:**
```bash
git clone https://ваш-репозиторий/anomaly.git
cd anomaly
```

*Что делает эта команда: `cd` означает "перейти в папку". Это как открыть папку в проводнике.*

### Шаг 2: Установка необходимых программ (зависимостей)

В том же терминале выполните:

```bash
pip install -r requirements.txt
```

*Эта команда скачает и установит все необходимые дополнительные программы. Это может занять несколько минут.*

**Если команда не работает, попробуйте:**
```bash
python -m pip install -r requirements.txt
```

### Шаг 3: Настройка конфигурации

1. Откройте папку `config` в проекте
2. Найдите файл `agents.yaml.example`
3. Скопируйте его и переименуйте копию в `agents.yaml`

**Как скопировать файл:**
- На Windows: Правый клик → Копировать, затем Вставить и переименовать
- На Mac/Linux: В терминале: `cp config/agents.yaml.example config/agents.yaml`

4. Откройте файл `agents.yaml` в любом текстовом редакторе (Блокнот, TextEdit и т.д.)

5. В файле вы увидите примеры LLM-агентов. Измените их под свои нужды:

```yaml
agents:
  - agent_id: gpt4_chatbot
    metrics_endpoint: http://localhost:9090/metrics
    metadata:
      name: "GPT-4 Chatbot"
      model: "gpt-4"
      provider: "openai"
      environment: "production"
      context_window_size: 8192
      rate_limit: 500
```

*Что означают эти строки:*
- `agent_id`: Уникальное имя вашего LLM-агента (используйте только буквы, цифры и подчеркивания)
- `metrics_endpoint`: Адрес, где система может получить данные о работе агента
- `name`: Понятное название
- `model`: Название модели LLM (например, "gpt-4", "claude-3-opus")
- `provider`: Провайдер модели ("openai", "anthropic", "google")
- `context_window_size`: Размер контекстного окна в токенах
- `rate_limit`: Лимит запросов в минуту
- `environment`: Где работает программа (production, staging, development)

## Запуск системы

### Вариант 1: Простой запуск (с веб-интерфейсом)

В терминале выполните:

```bash
python main.py
```

Система начнет работать и будет доступна по адресу: http://localhost:8000

*localhost означает "этот компьютер", 8000 - это номер порта (как номер комнаты в здании)*

### Вариант 2: Запуск с настройками

```bash
python main.py --config config/config.yaml --agents-config config/agents.yaml
```

*Это говорит системе использовать ваши конфигурационные файлы*

### Вариант 3: Запуск без веб-интерфейса (только мониторинг)

```bash
python main.py --no-api
```

*В этом режиме система только следит за агентами, но не предоставляет веб-интерфейс*

## Как использовать веб-интерфейс

После запуска системы откройте браузер и перейдите на:

```
http://localhost:8000
```

Вы увидите список доступных функций:

### Проверка, что система работает

Откройте: http://localhost:8000/health

Должно появиться сообщение: `{"status": "healthy"}`

### Просмотр списка агентов

Откройте: http://localhost:8000/agents

Вы увидите список всех зарегистрированных агентов.

### Просмотр обнаруженных проблем (аномалий)

Откройте: http://localhost:8000/anomalies

Вы увидите список всех найденных проблем.

**Можно добавить фильтры:**
- За последние 24 часа: http://localhost:8000/anomalies?hours=24
- Для конкретного агента: http://localhost:8000/anomalies?agent_id=мой_агент_1
- Только серьезные проблемы: http://localhost:8000/anomalies?min_severity=0.7

### Статистика

- Общая статистика: http://localhost:8000/stats
- Статистика по агенту: http://localhost:8000/agents/мой_агент_1/stats

## Настройка правил обнаружения проблем

В файле `config/rules.yaml` вы можете настроить, какие ситуации считать проблемами для ваших LLM-агентов.

### Примеры правил для LLM-агентов:

**Высокое использование токенов:**
```yaml
rules:
  - name: high_token_usage
    description: Использование токенов превышает порог
    anomaly_type: token_usage
    severity: 0.8
    logic: AND
    conditions:
      - metric: total_tokens
        operator: ">"
        value: 50000
```

**Низкое качество ответов:**
```yaml
  - name: quality_degradation
    description: Качество ответов упало
    anomaly_type: quality
    severity: 0.7
    logic: AND
    conditions:
      - metric: quality_score
        operator: "<"
        value: 0.6
```

**Высокая задержка:**
```yaml
  - name: very_high_latency
    description: Задержка ответа слишком высокая
    anomaly_type: latency
    severity: 0.9
    logic: AND
    conditions:
      - metric: latency_ms
        operator: ">"
        value: 60000
```

**Обнаружена галлюцинация:**
```yaml
  - name: hallucination_detected
    description: Обнаружена галлюцинация в ответе
    anomaly_type: quality
    severity: 0.9
    logic: AND
    conditions:
      - metric: hallucination_detected
        operator: "=="
        value: true
```

*Что это означает:*
- `anomaly_type`: Тип аномалии (token_usage, latency, cost, quality, rate_limit, context_overflow)
- `severity`: Серьезность проблемы (от 0 до 1, где 1 - критическая)
- `metric`: Название метрики для проверки
- `operator`: Оператор сравнения (>, <, ==, >=, <=)
- `value`: Пороговое значение

### Операторы для условий

- `>` - больше чем
- `>=` - больше или равно
- `<` - меньше чем
- `<=` - меньше или равно
- `==` - равно
- `!=` - не равно

## Частые проблемы и их решение

### Проблема: "python: command not found"

**Решение:** Python не установлен или не добавлен в PATH. Переустановите Python и обязательно отметьте "Add Python to PATH".

### Проблема: "No module named 'xxx'"

**Решение:** Не установлены зависимости. Выполните:
```bash
pip install -r requirements.txt
```

### Проблема: "Port 8000 is already in use"

**Решение:** Порт 8000 уже занят другой программой. Запустите с другим портом:
```bash
python main.py --port 8001
```

### Проблема: "Agent not found"

**Решение:** Агент не зарегистрирован. Убедитесь, что вы добавили его в файл `agents.yaml` и перезапустили систему.

### Проблема: Не получается подключиться к metrics_endpoint

**Решение:** 
1. Проверьте, что адрес правильный
2. Убедитесь, что программа действительно работает и отвечает по этому адресу
3. Попробуйте открыть адрес в браузере - должны увидеть данные в формате JSON

## Использование Docker (для более опытных пользователей)

Docker - это способ упаковать программу со всеми зависимостями, чтобы она работала одинаково на любом компьютере.

### Установка Docker

1. Скачайте Docker Desktop с сайта: https://www.docker.com/products/docker-desktop
2. Установите и запустите Docker Desktop
3. Дождитесь, пока Docker полностью запустится (иконка в трее перестанет мигать)

### Запуск с Docker

1. Откройте терминал в папке проекта
2. Выполните:

```bash
docker-compose up -d
```

*Команда `-d` означает "в фоновом режиме" - система будет работать, даже если закрыть терминал*

### Остановка Docker

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f
```

*Команда `-f` означает "следить за новыми сообщениями" - как живой вывод*

### Доступ к системе в Docker

- API: http://localhost:8000
- Prometheus (для метрик): http://localhost:9090
- Grafana (для графиков): http://localhost:3000
  - Логин: `admin`
  - Пароль: `admin` (при первом входе попросит изменить)

## Что дальше?

### Примеры использования

В папке `examples` есть примеры программ, которые показывают, как использовать систему:

1. **basic_usage.py** - простой пример создания системы и регистрации агента
2. **agent_simulator.py** - симулятор, который создает виртуальных агентов и генерирует проблемы для тестирования

Чтобы запустить пример:

```bash
python examples/basic_usage.py
```

### Настройка уведомлений

По умолчанию система показывает проблемы в консоли. Вы можете настроить отправку уведомлений:

- **В Slack** - для командной работы
- **По Email** - для важных проблем
- **В Telegram** - для мобильных уведомлений

Для этого нужно изменить код в файле `main.py` или создать свой обработчик уведомлений.

### Получение помощи

Если что-то не работает:

1. Проверьте, что все правильно установлено (см. раздел "Частые проблемы")
2. Посмотрите сообщения об ошибках в терминале
3. Убедитесь, что используете правильные версии программ

## Глоссарий (словарь терминов)

- **API** - способ, как программы общаются друг с другом
- **LLM (Large Language Model)** - Большая языковая модель, например GPT-4, Claude
- **LLM-агент** - Автономная система на базе LLM, способная воспринимать окружение и планировать действия
- **Токены** - Единицы текста, на которые LLM разбивает входные данные. Больше токенов = выше стоимость
- **Галлюцинация** - Когда LLM выдает информацию, которая выглядит правдоподобно, но является неправильной
- **Контекстное окно** - Максимальное количество токенов, которые LLM может обработать за один раз
- **Rate Limit** - Лимит на количество запросов к API в единицу времени
- **Latency** - Задержка, время от отправки запроса до получения ответа
- **Конфигурация** - файлы с настройками системы
- **Логи** - записи о том, что происходило в системе
- **Метрики** - числовые данные о работе программы
- **Порт** - номер, который помогает компьютеру понять, к какой программе обращаться (как номер комнаты)
- **Терминал/Командная строка** - способ управлять компьютером через текст, а не через графический интерфейс
- **Эндпоинт** - адрес, по которому можно получить информацию

## Быстрая справка по командам

### Запуск системы
```bash
python main.py
```

### Остановка системы
Нажмите `Ctrl+C` в терминале

### Просмотр справки
```bash
python main.py --help
```

### Запуск с конкретными настройками
```bash
python main.py --config путь/к/config.yaml --port 8001
```

### Просмотр установленных пакетов
```bash
pip list
```

### Обновление пакетов
```bash
pip install --upgrade -r requirements.txt
```

---

**Удачи в использовании системы!** 🚀

Если возникнут вопросы, не стесняйтесь обращаться за помощью.

