# Система обнаружения аномалий для LLM-агентов

Комплексная система обнаружения аномалий, разработанная для мониторинга и анализа поведения **LLM-агентов** — автономных систем на базе больших языковых моделей (Large Language Models), способных воспринимать окружение, планировать действия и достигать целей без жесткого программирования.

## 📚 Документация для начинающих

**Если вы новичок в программировании:**
- 🚀 [Быстрый старт (5 минут)](QUICK_START.md) - начните здесь!
- 📘 [Простая документация](README_SIMPLE.md) - подробное объяснение простым языком
- 🔧 [Подробное руководство по установке](INSTALL_GUIDE.md) - пошаговые инструкции
- 📋 [Шпаргалка команд](CHEATSHEET.md) - быстрая справка
- 🇷🇺 [Русская документация](README_RU.md) - навигация по всем документам

**Для опытных разработчиков** - продолжайте читать ниже ↓

## Основные возможности

- 🤖 **Специализация для LLM-агентов**: оптимизировано для мониторинга больших языковых моделей
- 🔍 **Множественные методы обнаружения**: статистические, ML, правил-основанные и временные ряды
- 💰 **Мониторинг стоимости**: отслеживание расходов на API запросы
- ⏱️ **Мониторинг производительности**: задержки, использование токенов, rate limits
- 📊 **Контроль качества**: обнаружение галлюцинаций, фактологических ошибок, оценка качества ответов
- 📊 **Реал-тайм мониторинг**: непрерывный сбор метрик и обнаружение аномалий
- 🚨 **Система оповещений**: настраиваемые каналы уведомлений
- 📈 **Интеграция с Prometheus**: экспорт метрик для мониторинга
- 🌐 **REST API**: полный API для управления системой
- 📡 **WebSocket**: real-time уведомления об аномалиях
- 🐳 **Docker поддержка**: готовое к развертыванию решение

## LLM-специфичные метрики

Система отслеживает следующие метрики, специфичные для LLM-агентов:

- **Токены**: использование input/output/total токенов, скачки потребления
- **Задержка (Latency)**: время ответа модели, аномальные задержки
- **Стоимость (Cost)**: расходы на API, дневные лимиты бюджета
- **Качество (Quality)**: качество ответов, галлюцинации, фактологические ошибки
- **Rate Limits**: мониторинг лимитов API запросов
- **Context Window**: использование контекстного окна, риск переполнения

## Архитектура

Система построена по модульной архитектуре с четким разделением ответственности между компонентами.

**📖 [Подробное описание архитектуры](ARCHITECTURE.md)**

### Обзор компонентов

1. **Core Layer (Ядро системы)**:
   - `Anomaly`: Dataclass для представления аномалий
   - `AnomalyDetector`: Абстрактный базовый класс для детекторов
   - `AgentMonitor`: Мониторинг отдельных агентов
   - `LLMAgent`: Класс для представления LLM-агентов
   - `LLMAgentMetrics`: Структура метрик для LLM-агентов

2. **Detectors Layer (Слой детекторов)**:
   - `StatisticalDetector`: Z-score, IQR, скользящее среднее
   - `MLDetector`: Isolation Forest, One-Class SVM
   - `RuleBasedDetector`: Конфигурируемые правила
   - `TimeSeriesDetector`: Анализ временных рядов с Prophet
   - **LLM-специфичные детекторы**:
     - `TokenUsageDetector`: Аномалии использования токенов
     - `LatencyDetector`: Аномалии задержки ответов
     - `CostDetector`: Аномалии стоимости API
     - `QualityDetector`: Снижение качества ответов
     - `RateLimitDetector`: Превышение rate limits
     - `ContextOverflowDetector`: Переполнение контекстного окна

3. **Management Layer (Слой управления)**:
   - `AnomalyRegistry`: Регистрация и дедупликация аномалий
   - `AlertManager`: Управление оповещениями (консоль, Slack, Email, Webhook)
   - `MetricsCollector`: Сбор и хранение метрик

4. **System Layer (Системный слой)**:
   - `AnomalyDetectionSystem`: Главный координатор всей системы

5. **API Layer (Слой API)**:
   - REST API (FastAPI) для управления и получения данных
   - WebSocket для real-time уведомлений
   - Prometheus exporter для метрик

### Архитектурные паттерны

- **Strategy Pattern** - различные алгоритмы детекции через единый интерфейс
- **Observer Pattern** - уведомления через AlertManager
- **Repository Pattern** - инкапсуляция логики хранения в Registry
- **Factory Pattern** - создание детекторов из конфигурации
- **Chain of Responsibility** - последовательная обработка детекторами

### Потоки данных

```
Сбор метрик → Детекторы → Регистрация → Алерты
                     ↓
              REST API / WebSocket
                     ↓
                 Клиенты
```

См. [ARCHITECTURE.md](ARCHITECTURE.md) для детального описания.

## Установка

### Требования

- Python 3.9+
- pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Конфигурация

1. Скопируйте пример конфигурации агентов:
```bash
cp config/agents.yaml.example config/agents.yaml
```

2. Отредактируйте `config/config.yaml` и `config/rules.yaml` под ваши нужды

## Использование

### Базовое использование для LLM-агентов

```python
from anomaly_detection import AnomalyDetectionSystem

# Создание системы с LLM-конфигурацией
system = AnomalyDetectionSystem(config='config/config.yaml')

# Регистрация LLM-агента
agent = system.register_agent(
    agent_id='gpt4_chatbot',
    metrics_endpoint='http://localhost:9090/metrics',
    metadata={
        'model': 'gpt-4',
        'provider': 'openai',
        'context_window_size': 8192,
        'rate_limit': 500
    }
)

# Запуск мониторинга
await system.start_monitoring()

# Получение аномалий
anomalies = system.get_recent_anomalies(hours=24)
for anomaly in anomalies:
    print(f"Anomaly: {anomaly.description}, Severity: {anomaly.severity}")
```

### Использование класса LLMAgent (опционально)

```python
from anomaly_detection import AnomalyDetectionSystem, LLMAgent

# Создание LLM-агента с типизацией
llm_agent = LLMAgent(
    agent_id='claude_assistant',
    model='claude-3-opus',
    provider='anthropic',
    context_window_size=200000,
    rate_limit=50,
    metadata={'environment': 'production'}
)

system = AnomalyDetectionSystem(config='config/config.yaml')
system.register_agent(llm_agent.agent_id, metadata=llm_agent.metadata)
```

### Пример метрик LLM-агента

```python
# Метрики, которые собирает система для LLM-агентов
metrics = {
    "input_tokens": 1500,
    "output_tokens": 800,
    "total_tokens": 2300,
    "latency_ms": 2500.0,
    "cost_usd": 0.04,
    "quality_score": 0.92,
    "coherence_score": 0.90,
    "relevance_score": 0.91,
    "context_window_usage": 0.28,
    "hallucination_detected": False,
    "factual_errors": 0,
    "model": "gpt-4",
    "provider": "openai",
    "rate_limit_remaining": 450,
}
```

### Запуск с API сервером

```bash
python main.py --config config/config.yaml --agents-config config/agents.yaml
```

### Запуск без API (только мониторинг)

```bash
python main.py --config config/config.yaml --no-api
```

### Использование Docker Compose

```bash
docker-compose up -d
```

Система будет доступна по адресам:
- API: http://localhost:8000
- Prometheus: http://localhost:9090
- Grafana: http://localhost:3000 (admin/admin)

## API Endpoints

### Агенты

- `POST /agents/register` - Регистрация нового агента
- `GET /agents` - Список всех агентов
- `GET /agents/{agent_id}` - Информация об агенте
- `DELETE /agents/{agent_id}` - Удаление агента
- `POST /agents/{agent_id}/check` - Ручная проверка агента

### Аномалии

- `GET /anomalies` - Получить список аномалий
  - Параметры: `hours`, `agent_id`, `anomaly_type`, `min_severity`, `limit`
- `GET /agents/{agent_id}/stats` - Статистика по агенту
- `GET /stats` - Общая статистика системы

### Мониторинг

- `POST /monitoring/start` - Запустить мониторинг
- `POST /monitoring/stop` - Остановить мониторинг
- `GET /health` - Health check

### Метрики

- `GET /metrics` - Prometheus метрики

### WebSocket

- `WS /ws/anomalies` - Real-time поток аномалий

## Примеры

### Пример 1: Базовое использование

См. `examples/basic_usage.py`

```bash
python examples/basic_usage.py
```

### Пример 2: Симулятор LLM-агентов

См. `examples/llm_agent_simulator.py`

```bash
python examples/llm_agent_simulator.py
```

### Пример 3: Работа с LLM-агентом

См. `examples/llm_agent_example.py`

```bash
python examples/llm_agent_example.py
```

## Конфигурация детекторов

### Statistical Detector

```yaml
detectors:
  statistical:
    enabled: true
    z_score_threshold: 3.0
    iqr_multiplier: 1.5
    window_size: 100
```

### ML Detector

```yaml
detectors:
  ml:
    enabled: true
    method: isolation_forest  # or "one_class_svm"
    contamination: 0.1
    n_estimators: 100
    online_learning: false
```

### Rule-Based Detector

Создайте правила в `config/rules.yaml`:

```yaml
rules:
  - name: high_cpu_usage
    description: CPU usage exceeds 90%
    anomaly_type: performance
    severity: 0.8
    logic: AND
    conditions:
      - metric: cpu_usage
        operator: ">"
        value: 90.0
```

### Time Series Detector

```yaml
detectors:
  timeseries:
    enabled: true
    window_size: 100
    use_prophet: true
    trend_threshold: 2.0
```

## Типы аномалий

### Общие типы:
- `BEHAVIORAL` - Поведенческие аномалии
- `PERFORMANCE` - Проблемы производительности
- `COMMUNICATION` - Проблемы коммуникации
- `TEMPORAL` - Временные аномалии
- `SEMANTIC` - Семантические аномалии

### LLM-специфичные типы:
- `TOKEN_USAGE` - Аномальное использование токенов (скачки, превышение лимитов)
- `LATENCY` - Аномальная задержка ответа (слишком долгие ответы)
- `COST` - Аномальные расходы на API (превышение бюджета)
- `QUALITY` - Падение качества ответов (галлюцинации, ошибки)
- `RATE_LIMIT` - Превышение лимитов API запросов
- `CONTEXT_OVERFLOW` - Переполнение контекстного окна

## Метрики Prometheus

Система экспортирует следующие метрики:

- `anomalies_detected_total` - Общее количество обнаруженных аномалий
- `anomaly_severity` - Распределение severity аномалий
- `agents_monitored` - Количество мониторируемых агентов
- `active_anomalies` - Количество активных аномалий

## Дополнительная документация

- 📐 [ARCHITECTURE.md](ARCHITECTURE.md) - Подробное описание архитектуры системы
- 🤖 [README_LLM.md](README_LLM.md) - Руководство по работе с LLM-агентами
- 📚 [README_SIMPLE.md](README_SIMPLE.md) - Документация простым языком
- 🚀 [QUICK_START.md](QUICK_START.md) - Быстрый старт

## Производительность

Система оптимизирована для:
- Низкой задержки обнаружения (< 1 секунда)
- Высокой пропускной способности (10K+ метрик/сек)
- Минимального потребления памяти
- Горизонтального масштабирования

## Расширение системы

### Добавление нового детектора

```python
from anomaly_detection.core.detector import AnomalyDetector
from anomaly_detection.core.anomaly import Anomaly, AnomalyType

class CustomDetector(AnomalyDetector):
    def detect(self, agent_id: str, metrics: dict, historical_data=None):
        # Ваша логика обнаружения
        anomalies = []
        # ...
        return anomalies

# Использование
system.add_detector(CustomDetector(name="custom"))
```

### Кастомный обработчик алертов

```python
from anomaly_detection.management.alert_manager import AlertManager, AlertChannel

async def my_slack_handler(anomaly, message):
    # Ваша логика отправки в Slack
    pass

alert_manager.register_handler(AlertChannel.SLACK, my_slack_handler)
```

## Разработка

### Запуск тестов

```bash
pytest tests/
```

### Форматирование кода

```bash
black anomaly_detection/
```

## Лицензия

MIT

## Авторы

Система разработана для использования в распределенных мультиагентных системах.

## Поддержка

Для вопросов и предложений создайте issue в репозитории проекта.

