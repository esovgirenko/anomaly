global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'anomaly-detection'
    static_configs:
      - targets: ['anomaly-detection:8000']
    metrics_path: '/metrics'

